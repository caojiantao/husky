spring:
  # 数据库连接
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/husky?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true
    username: root
    password: Cjt201802136.
  # redis配置
  redis:
    host: 127.0.0.1
    port: 6379
    password:
# mybatis配置
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
