spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/husky?useSSL=false&useUnicode=true&characterEncoding=UTF-8&autoReconnect=true
    username: root
    password: 123456

  redis:
    host: 127.0.0.1
    password: 123456

  http:
    encoding:
      force: true
      charset: UTF-8
  servlet:
    multipart:
      max-file-size: 10Mb

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml

server:
  port: 8080